services:
  postgres:
    image: 'postgres:17'
    container_name: 'test-db'
    ports:
      - "15002:5432"
    environment:
      POSTGRES_USER: test
      POSTGRES_PASSWORD: test
      POSTGRES_DB: testdb
    volumes:
      - /var/lib/postgresql/data


