package com.finwise.customers.domain.enums;

/**
 * Enum to represent whether a customer is an individual person or a group (e.g. solidarity group).
 */
public enum CustomerType {

    /**
     * A single individual customer.
     */
    INDIVIDUAL,

    /**
     * A group of customers acting collectively (e.g. group loan).
     */
    GROUP
}
